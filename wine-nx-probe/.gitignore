build-switch/
build-wine-arm64-pe/
build-wine-arm64-pe-clean/
build-wine-arm64-pe-local/
toolchains/
# Sample binaries are fetched (sha256-pinned), not tracked -- see
# tools/fetch-samples.sh.
samples/**/*.exe
samples/**/*.dll
samples/*.zip
